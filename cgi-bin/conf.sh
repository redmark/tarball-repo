##### common app param

#directory of the entire app on the linux system, all bash cgi script must use this variable to locate resources on file system
APP_DIR="$DOCUMENT_ROOT"

#dir partial uri, all url must start with it
APP_PATH=""